"""
Code for generating REST API schema files for use in the ``django-user-tasks`` documentation.

Note that docs/swagger.json has been hand-edited after generation, so take
care not to blindly replace it with a newly generated copy.  Future
enhancements to django-rest-swagger may allow generation of files which
require less manual tweaking.
"""
